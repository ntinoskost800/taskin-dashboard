import { Theme } from '@mui/material'

const getBorderColor = (theme: Theme) => '#eee'

export const getBorderColor2 = (theme: Theme) => '#ddd'

// Export the most usual
export default getBorderColor
